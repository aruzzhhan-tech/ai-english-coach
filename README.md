# AI English Coach

Static GitHub Pages prototype based on the PRD.

The app demonstrates:

- dashboard with local progress
- daily lesson generator
- English-only conversation coach simulation
- mistake tracking
- vocabulary manager
- browser-local memory through `localStorage`

The original PRD describes a FastAPI + Ollama desktop app. GitHub Pages cannot run that backend, so this repository ships a static version that can be hosted directly from GitHub.
